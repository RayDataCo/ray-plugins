---
description: Say hello from the Ray Data Co example plugin
---

Greet the user warmly on behalf of the Ray Data Co example plugin. In one sentence, explain that this `/hello` command was installed from the `ray-plugins` marketplace and that they can copy `plugins/rdco-example/` as a template for their own plugin. If the user passed a name in $ARGUMENTS, greet them by that name.
